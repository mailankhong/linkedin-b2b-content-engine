# Environment variables — never commit these
.env
.env.local
.env.production

# Claude Code local settings — contains API keys, never commit
.claude/settings.json
.claude/settings.local.json
.mcp.json

# macOS
.DS_Store

# Node
node_modules/
